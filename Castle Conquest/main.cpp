#include <iostream>
using namespace std;

#include "BST.h"

int main()
{
	BSTNode<int> temp(0);
	BST<int> kemp;
	kemp.insert(0);
	kemp.insert(111111111);
	kemp.insert(102565);
	kemp.insert(-256);
	return 0;
}